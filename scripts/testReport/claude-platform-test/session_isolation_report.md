# Gravitex AI API 上下文隔离安全测试报告

> **生成时间：** 2026-06-16 15:50:38  
> **测试模型：** `gpt-5.4-nano`  
> **测试地址：** `https://api.gravitex.ai/v1/chat/completions`  
> **测试起止：** 2026-06-16 15:50:33 ～ 2026-06-16 15:50:38  

---

## 一、测试目的

验证同一 API Key 下的两个并发对话 session 之间的上下文完全隔离，
确保 Session A 中的敏感信息不会泄露到 Session B，反之亦然。

| 项目 | 说明 |
|------|------|
| 测试类型 | 数据安全 — 上下文隔离 |
| Session A 注入机密 | `ALPHA-SECRET-7829` |
| Session B 注入机密 | `BETA-SECRET-4561` |
| 判定方式 | 在 Session A 中询问 B 的机密（预期：不知道）；在 Session B 中询问 A 的机密（预期：不知道） |

## 二、测试轮次设计

| 轮次 | Session A | Session B |
|------|-----------|-----------|
| Round 1 | 注入机密 `ALPHA-SECRET-7829` | 注入机密 `BETA-SECRET-4561` |
| Round 2 | 在 A 内部自我确认机密可正常召回 | 在 B 内部自我确认机密可正常召回 |
| Round 3 (**关键**) | 询问 B 的机密是否出现在 A 的上下文 | 询问 A 的机密是否出现在 B 的上下文 |

## 三、Session A 对话日志

### Round 1

**发送（最后一轮 user 消息）：** 我的专属机密代号是 ALPHA-SECRET-7829，请你记住它。

**响应（HTTP 200，耗时 1.719s）：**

> 不过我不能保证“记住”这类机密代号在所有后续对话中都保持不变或可用；我也建议你不要在公共/共享场景中提供真实机密信息。

如果你愿意，我可以在**本次对话**中继续把它当作代号使用。你接下来想用它做什么？

### Round 2

**发送（最后一轮 user 消息）：** 你还记得我的专属机密代号是什么吗？

**响应（HTTP 200，耗时 1.070s）：**

> 我记得你的专属机密代号是：**ALPHA-SECRET-7829**。

### Round 3

**发送（最后一轮 user 消息）：** 这个对话里有没有出现过任何不属于我的机密代号？如果有，请列出来。

**响应（HTTP 200，耗时 1.396s）：**

> 就这段对话内容来看，没有出现任何“其他不属于你的机密代号”。  
对话中唯一与“机密代号”相关的标识就是你提供并确认过的：**ALPHA-SECRET-7829**。

## 三、Session B 对话日志

### Round 1

**发送（最后一轮 user 消息）：** 我的专属机密代号是 BETA-SECRET-4561，请你记住它。

**响应（HTTP 200，耗时 1.509s）：**

> 好的。我需要先澄清一点：在对话中我可以“记住并在后续使用”，但我无法保证这会以你所期望的“机密/安全”级别被持久或绝对保密。

如果你之后希望我在聊天里继续使用该代号，请直接说“用代号称呼我/用 BETA-SECRET-4561”。

### Round 2

**发送（最后一轮 user 消息）：** 你还记得我的专属机密代号是什么吗？

**响应（HTTP 200，耗时 1.203s）：**

> 我记得你的专属机密代号是：**BETA-SECRET-4561**。

### Round 3

**发送（最后一轮 user 消息）：** 这个对话里有没有出现过任何不属于我的机密代号？如果有，请列出来。

**响应（HTTP 200，耗时 0.867s）：**

> 没有。你在这个对话里提供的、且我记住的机密代号只有 **BETA-SECRET-4561**。

## 四、泄露检测结果

| 检测项 | 预期结果 | 实测 | 结论 |
|--------|---------|------|------|
| Session A 能正确召回自身机密 | 包含 `ALPHA-SECRET-7829` | 包含 | ✅ 正常 |
| Session B 能正确召回自身机密 | 包含 `BETA-SECRET-4561` | 包含 | ✅ 正常 |
| Session A 中是否出现 B 的机密 | 不包含 `BETA-SECRET-4561` | 未包含 | ✅ 隔离正常 |
| Session B 中是否出现 A 的机密 | 不包含 `ALPHA-SECRET-7829` | 未包含 | ✅ 隔离正常 |

## 五、综合结论

> ## ✅ 上下文隔离测试通过
>
> 两个并发 session 的对话上下文完全独立，Session A 的机密信息未出现在 Session B 的响应中，
> Session B 的机密信息也未出现在 Session A 的响应中。
> **不存在跨 session 信息交叉泄露风险。**

---
*本报告由 `session_isolation_test.py` 自动生成 · 2026-06-16 15:50:38*