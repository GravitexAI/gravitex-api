# 阶段4 relaykit 迁移 —— 进行中工作记录

> ✅ **阶段4 已完成**（2026-08-06）。本文件保留全部决策过程供追溯，最终结论见第十一节。

## 一、当前状态

- **merge 目标**：`origin/main`（最新），吸收剩余 **31 个** commit
- **merge 已发起，未 commit**（`git merge origin/main --no-ff --no-commit`）
- 冲突总数 **38 个**，已解决 **24 个**，剩 **14 个**

## 二、重大好消息：git 自动完成了 dto → relaykit/dto 重命名检测

原本预估「22 个 dto 文件要做依赖解耦」的最大风险**没有发生**。git 的 rename detection 把 main-alpha 对 `dto/*.go` 的改动自动跟随到了 `relaykit/dto/*.go`。

**已验证保住的 main-alpha 独有内容**：
- `relaykit/dto/channel_settings.go`：`ByteplusAssetAK/SK/Region/ProjectName`、`AnthropicBetaTarget`、`AzureModelResponsesVersions`、`AzureModelApiVersions`、`EnableModerationQuery` —— 5 项全在
- `relaykit/dto/openai_request.go`：Qwen 扩展参数 `PreserveThinking`、`ThinkingBudget`、`SearchOptions`、`EnableCodeInterpreter`、`ToolStream` —— 5 项全在

**dto 目录现状**：`dto/` 只剩 5 个文件（midjourney/suno/task/video + openai_request_zero_value_test），其余 30 个已到 `relaykit/dto/`。

## 三、relaykit 架构关键事实

- **relaykit 是独立 Go module**：`relaykit/go.mod`，module path `github.com/QuantumNous/new-api/relaykit`
- 主 module 用 `replace github.com/QuantumNous/new-api/relaykit => ./relaykit` 引用
- relaykit 只依赖 5 个第三方包：`uuid`、`lo`、`testify`、`gjson`、`sjson` —— **不能 import `gin`/`common`/`model`/`setting`**
- 官方的依赖替代方案：
  - `types/` 错误类型 → 复制进 `relaykit/types/`（`OpenAIError`/`ClaudeError`/`NewAPIError`/`ErrorCode` 等）
  - `common` 工具函数 → `relaykit/relayconvert/kitutil/`（json/log/mask/value）
  - `gin.Context` → `context.Context`
  - `*relaycommon.RelayInfo` → `convmeta.Meta` 接口
  - 全局 settings → `convmeta.Options` 快照

### types 包分家规则（重要）

| 类型 | 归属 |
|---|---|
| `OpenAIError` / `ClaudeError` / `NewAPIError` / `ErrorType` / `ErrorCode` / `FileSource` / `RelayFormat` / `EndpointType` / `ChannelError` / `RequestMeta` | `relaykit/types/` |
| **`PriceData`** / `RWMap` / `Set` | **主 module `types/`（不变）** |

官方在同时用两者的文件里，把主 module 的 types 起别名为 **`hosttypes`**。

`relaykit/relayconvert` 的 18 个子包：`dto` `reasonmap` `relayconvert{,/convmeta,/internal/{claude_messages,gemini_chat,jsonutil,media,oai_chat,oai_responses,shared/claude,shared/gemini},/kitutil,/reasoning,/testdata/golden/{request,response,stream}}` `types`

## 四、已解决的 24 个冲突及决策

| 文件 | 决策 |
|---|---|
| `go.sum` | theirs（后 `go mod tidy`） |
| `web/src/features/channels/constants.ts` | **移除**（官方摊平结构文件，我们保 `web/default` + `web/classic`） |
| `middleware/trusted_proxies{,_test}.go` | **移除**（官方新 auth 子系统，阶段3 已定不引入） |
| `relay/channel/claude/tool_billing_test.go` | **移除**（依赖官方 claude 实现） |
| `dto/openai_request_zero_value_test.go` | ours |
| `relaykit/dto/openai_request.go` | ours 侧的 `PreserveThinking`（Qwen 扩展参数） |
| `relay/channel/claude/relay-claude.go` | **ours**（987 行定制，防丢清单 7.11.11） |
| `relay/channel/claude/relay_claude_test.go` | ours（配套） |
| `relay/channel/gemini/relay-gemini.go` | **ours**（1130 行定制，含 CHZ-PATCH 图文分离计费） |
| `constant/context_key.go` | ours（main-alpha 多 `ContextKeyTokenVendorLimit*` / `ContextKeyTokenCrossGroupRetry`） |
| `relay/common/relay_info.go` | **并集**：官方的 `LastMessageType* = convmeta.*` 别名（单一来源更好）+ 我们的 `TaskSubmitDelayResponse` / `TaskSubmitResponseBody` |
| `service/tiered_settle.go` | 并集 |
| `go.mod` | 并集（后 tidy） |
| `i18n/keys.go` | 并集 |
| `relay/channel/task/ali/adaptor.go` | **ours**（13 wan 模型 + 计费引擎，防丢清单 7.11.4） |
| `relay/channel/task/kling/adaptor.go` | **ours**（`AdjustBillingOnComplete`，防丢清单 7.11.5） |
| `relay/channel/aws/dto.go` | 官方（relaykit import 迁移） |
| `service/billing.go` | 官方（`types` → `relaykit/types`） |
| `service/log_info_generate.go` | 官方（`GenerateMjOtherInfo` 去 gin + `hosttypes.PriceData`） |
| `relay/channel/api_request.go` | 官方 |

## 五、剩余 14 个冲突（待处理）

```
controller/model_list_test.go
controller/relay.go
controller/token.go              (7 块)
main.go
model/log.go                     (删 reject_reason/stream_status/official_quota vs 官方注释掉)
model/main.go                    (3 块)
model/token.go                   (2 块)
relay/channel/aws/relay_aws_test.go
relay/channel/openai/relay_responses.go
relay/channel/task/doubao/adaptor.go
relay/channel/task/gemini/adaptor.go   (3 块)
relay/channel/task/vertex/adaptor.go
relay/gemini_handler.go
relay/helper/price.go            (main-alpha 图片按张计费定制)
service/task_polling_test.go
```

### 处理原则（已确立）

1. **纯 import 迁移**（`types` → `relaykit/types`、`dto` → `relaykit/dto`、加 `hosttypes` 别名）→ **取官方**，这是 relaykit 架构的正确形态
2. **main-alpha 计费定制** → 保留，但要放进官方的新签名里
3. **model/log.go 的 `formatUserLogs` 删字段** → 取 ours（`reject_reason`/`stream_status`/`official_quota`/`official_video_price_per_second` 是 admin-only，必须对用户隐藏）
4. task adaptor 的 doubao/gemini/vertex → 逐个看，计费相关保 ours

## 六、完成后必须做的机械核对

阶段4 涉及计费路径重构，**`go build` + `go test` 通过不等于计费正确**。必须逐项 grep 确认：

```bash
# main-alpha 独有计费符号
ImagePerImagePricing / PerImageUnitPrice / EstimateImagePerImageCost / ImagePriceMultiplier
ImageBillingUsage / VideoRatio / GeminiImageOutputTokens / GeminiTextOutputTokens
EffectiveImageOutputRatio / CachedTokensDetails / VideoTokens
AdjustBillingOnSubmit / AdjustBillingOnComplete / BillingResolutionKeyFromParams
CacheCreationTokensTotal（禁止改回相加）
handleVideoPerSecondBilling / handleVideoTokenRatioBilling
```

以及[防丢清单第九节](../main-alpha_独有功能清单.md)全部 39 项。

## 七、🔴 阶段4 完成后必须真机验证（不可跳过）

阶段4 动的是**协议转换 + 计费核心**，改错不会 build fail，会**静默算错钱**。已知这块没有测试兜底（17 个已知失败测试里 4 个就是 gemini 计费相关）。

必须在测试环境实测：
- [ ] Claude prompt cache 计费（`cache_creation` / `cache_read` / GPT-5.6 `cache_write_tokens` 取 max 不相加）
- [ ] Gemini 图文分离计费（`image_output_tokens` / `text_output_tokens` / `output_image_price`）
- [ ] Gemini Omni 视频输入计费
- [ ] 视频按秒计费（veo / 可灵 / 豆包）+ 按量计费
- [ ] gpt-image 按张计费（`/v1/images/generations` + `/v1/images/edits`）
- [ ] ali wan 系列视频计费（13 个模型）
- [ ] 阶梯计费表达式
- [ ] RuoYi JWT SSO（Java 跳转免登录）
- [ ] 双前端切换（classic ↔ default）

---

# 【存档更新 2026-08-05 第二次】38 个冲突已全解，剩编译错误收尾

## 八、冲突全部解决（38/38）

新增解决的 14 个（接第四节的 24 个）：

| 文件 | 决策 |
|---|---|
| `model/log.go` | ours（`formatUserLogs` 必须删 `reject_reason`/`stream_status`/`official_quota`/`official_video_price_per_second`，admin-only 字段对用户隐藏） |
| `relay/gemini_handler.go` | 官方（import 迁移） |
| `relay/channel/openai/relay_responses.go` | 官方（import 迁移） |
| `relay/helper/price.go` | **官方新签名 + main-alpha 图片按张计费实现**（`EstimateImagePerImageCost`/`ImagePerImagePricing`/`PerImageUnitPrice`/`ImagePriceMultiplier` 全保留，返回类型改 `hosttypes.PriceData`） |
| `relay/channel/task/{doubao,gemini,vertex}/adaptor.go` | 官方 `taskdto` 别名 import + **ours 的视频计费实现体** |
| `controller/token.go` | **ours**（保企业主账号 apikey 限制 2 处 + `Int64Flexible`）。⚠️ **官方的 token 自动分组功能未吸收**，见第十节待办 |
| `controller/relay.go` | 官方 `taskdto` 签名 + **ours 的 Seedance 原样透传逻辑**（`KeySeedanceRawMirror`） |
| `model/main.go` | ours（ByteHouse skip AutoMigrate + `MYSQL_PREPARE_STMT`） |
| `model/token.go` | ours struct + **手工补 `AutoGroups string` 字段**（官方自动分组的 model 层） |
| `main.go` | ours（官方 `ConfigureTrustedProxies` 依赖已按阶段3 决策移除的 `middleware/trusted_proxies.go`） |
| `relay/channel/aws/relay_aws_test.go` | 并集（保 main-alpha 的 beta 过滤回归测试 + 官方新增） |
| `controller/model_list_test.go` / `service/task_polling_test.go` | ours（官方新增内容依赖官方实现） |
| `i18n/keys.go` | 并集后**去重**（并集造成 7 个 `Msg*` 重复声明，脚本去重后保留 235 个） |
| `constant/context_key.go` | ours + **手工补 `ContextKeyTokenAutoGroups`**（官方自动分组需要） |

## 九、import 迁移规则（已执行，可复用）

### 9.1 dto 包分流

`dto/` 只保留 4 个文件的类型（**用这些的不改 import**）：
```
midjourney.go: MidjourneyDto MidjourneyRequest MidjourneyResponse MidjourneyResponseWithStatusCode
               MidjourneyStatus MidjourneyUploadResponse MidjourneyWithoutStatus ActionButton
               FetchReq ImgUrls Properties SwapFaceRequest GoAPI*
suno.go:       SunoDataResponse SunoSubmitReq SunoLyrics SunoMetadata SunoSong SunoGoAPISubmitReq*
task.go:       TaskData TaskDto TaskError TaskResponse
video.go:      VideoRequest VideoResponse VideoTaskError VideoTaskMetadata VideoTaskResponse
```

其余 30 个文件的类型 → `relaykit/dto`。

**已执行的分流**（脚本按"文件里实际用了哪些 `dto.X`"自动判断）：
- **22 个**只用新类型 → `sed` 改成 `"github.com/QuantumNous/new-api/relaykit/dto"`
- **6 个**混用 → 加 `taskdto "github.com/QuantumNous/new-api/dto"` 别名 + 旧类型引用改 `taskdto.`
  （`relay/channel/task/{gemini,kling,uptoken,seedancegateway,vertex,ali}/adaptor.go`）
- **11 个**只用旧类型 → 不动
- 另有 **10 个**官方已改用 `taskdto` 但被我误 sed 掉 import 的文件，已补回
  （`middleware/distributor.go`、`controller/relay.go`、`service/{error,task_polling,task_polling_test}.go`、`relay/channel/adapter.go`、`relay/channel/task/{hailuo,doubao,jimeng,vidu}/adaptor.go`）

### 9.2 types 包分流（**剩余编译错误就是这个**）

| 类型 | 归属 | import |
|---|---|---|
| `NewError` / `NewAPIError` / `ErrorCode*` / `ErrOption*` / `OpenAIError` / `ClaudeError` / `WithOpenAIError` / `FileSource` / `NewURLFileSource` / `RelayFormat*` / `EndpointType*` / `ChannelError` / `NewChannelError` / `RequestMeta` | **`relaykit/types`** | `"github.com/QuantumNous/new-api/relaykit/types"` |
| **`PriceData`** / `RWMap` / `Set` / `TokenCountMeta` | 主 module `types/` | 官方惯例起别名 `hosttypes "github.com/QuantumNous/new-api/types"` |

⚠️ **`sed` 的 `\b` 边界在 `types.PriceData{` 这种场景不可靠**，要用 perl 的 lookbehind：
```bash
perl -pi -e 's/(?<![A-Za-z0-9_.])types\.PriceData\b/hosttypes.PriceData/g' <file>
```

### 9.3 reasonmap 包移动
`relay/reasonmap` → `relaykit/reasonmap`（已改，仅 `relay/channel/claude/relay-claude.go` 一处）

## 十、🔴 剩余工作（下次会话从这里接）

### 10.1 剩余编译错误（6 个文件，规律一致）

这 6 个文件是**我取 ours 保留的 main-alpha 定制文件**，它们 import 主 module `types` 但用的是已迁到 `relaykit/types` 的错误类型：

```
controller/native_interactions.go
relay/channel/gemini/relay-gemini.go
relay/channel/claude/media_source.go
relay/channel/claude/relay-claude.go
relay/channel/claude/stream_server_tool_use_test.go
relay/channel/tokenhub/adaptor.go
```

**修法**（每个文件都要先读再改，不要批量正则）：
1. 看该文件用了哪些 `types.X`
2. 按 9.2 的表分类：错误类型 → 加 `"github.com/QuantumNous/new-api/relaykit/types"`；`PriceData`/`TokenCountMeta` → 加 `hosttypes` 别名
3. 如果两类都用，就同时 import 两个包，把 `PriceData` 那类改成 `hosttypes.`
4. `gofmt -w` 后 `go build ./...` 验证

预计还有几轮 `go build` 迭代（每次会暴露新的下游文件）。

### 10.2 编译通过后

- [ ] `go mod tidy`（主 module）+ `cd relaykit && go mod tidy`
- [ ] `go vet ./...` 修 test build 错误（同样规律：取 ours 或移除官方新增测试）
- [ ] `go test ./...`，对照阶段3 的 17 个已知失败，确认**无新增回归**
- [ ] 跑第六节的机械核对（main-alpha 独有计费符号）
- [ ] 跑[防丢清单第九节](../main-alpha_独有功能清单.md)全部 39 项
- [ ] commit（**注意保持 merge 状态，不要 stash / 不要 amend**）
- [ ] push

### 10.3 阶段4 新产生的待办（记入长期遗留）

- **官方 token 自动分组功能未吸收**：`controller/token.go` 取了 ours 保企业限制。官方引入了
  `tokenRequest` / `tokenResponse` / `tokenAutoGroupsInput` 包装类型，`buildMaskedTokenResponse`
  返回类型从 `*model.Token` 改成 `*tokenResponse`。model 层的 `AutoGroups` 字段和
  `ContextKeyTokenAutoGroups` 已补，但 controller 层没接。
  要接的话需把企业主账号限制（2 处）+ `Int64Flexible` 的 `updateTokenRequest` 织进官方新结构。
- **claude/gemini 的 2117 行定制仍未迁进 relaykit**：阶段2/3/4 都是取 ours 绕过。
  这是 relaykit 架构对齐的最后一块债，需要按 `convmeta.Meta` / `convmeta.Options` /
  `kitutil` 重写，且**必须真机验证计费**。


---

# 【阶段4 完工 2026-08-06】

## 十一、收尾做了什么

### 11.1 types 包 import 分流（第 10.1 节的 6 个文件）

全部只用 `relaykit/types` 的错误类型，无一用到 `PriceData`/`TokenCountMeta`，
所以不需要 `hosttypes` 双 import，直接换路径即可：

```
relay/channel/claude/{relay-claude,media_source,stream_server_tool_use_test}.go
relay/channel/gemini/relay-gemini.go
relay/channel/tokenhub/adaptor.go
controller/native_interactions.go
```

连锁暴露出的 4 个缺失 import（都是取 ours 时丢的，非官方改动）：

| 文件 | 缺的包 | 说明 |
|---|---|---|
| `relay/channel/openai/relay_responses.go` | `constant` | 官方版丢了这行 |
| `relay/gemini_handler.go` | `setting/ratio_setting` | 同上 |
| `relay/channel/aws/dto.go` | `relay/channel/claude` | 取官方 import 块时丢了 main-alpha 的 anthropic-beta 过滤依赖 |
| `controller/token.go` | `constant` / `setting` | 新增 auto-groups 端点需要 |

### 11.2 测试层 import 分流（`go vet` 暴露）

| 文件 | 处理 |
|---|---|
| `dto/openai_request_zero_value_test.go` | **删除**。git 重命名已把它搬到 `relaykit/dto/`（并改用 `kitutil`），这份是残留副本 |
| `relaykit/dto/openai_request_zero_value_test.go` | **补回丢失的 `prompt_cache_options` 断言**——搬迁时官方版覆盖掉了 main-alpha 的显式缓存控制覆盖 |
| `service/text_quota_test.go` | `types.PriceData`/`GroupRatioInfo` → `hosttypes.` |
| `relay/helper/image_billing_test.go` | `dto` → `relaykit/dto` |
| `controller/model_list_test.go` | 补 `relaykit/dto` / `setting` / `ratio_setting` import |
| `model/token_auto_groups_cache_test.go` | 补 `useUserCacheMiniRedis` helper（官方放在 `user_cache_auth_version_test.go`，那文件属阶段3 已拒的 auth 改写） |
| `service/tiered_settle_test.go` | `Settle(int)` → `Settle(int, bool)`，测试断言用户额度变化故传 `false` |

### 11.3 🔴 修掉一个合并时自己引入的回归：`Token.Update()` 丢了刷缓存

main-alpha 原本有异步刷缓存（`gopool.Go` + `cacheSetToken`），我在解冲突时**整段弄丢了**。
采用官方的**同步**版本（更好——收窄 `auto_groups`/`model_limits` 这类限制时，
异步刷新会留下一个仍按旧宽松配置放行的窗口），并上 main-alpha 的
`vendor_limits`/`daily_spend_threshold` 和官方的 `auto_groups`。

```go
err = DB.Model(token).Select("name", ..., "vendor_limits", ..., "daily_spend_threshold", "auto_groups").Updates(token).Error
if shouldUpdateRedis(true, err) {
    if cacheErr := cacheSetToken(*token); cacheErr != nil { ...; cacheDeleteToken(token.Key) }
}
```

### 11.4 补做了官方 token 自动分组功能（第 10.3 节原列为待办，已完成）

原打算延后，但依赖已齐（`service.IsUserSelectableGroup`、`setting.GetMaxTokenAutoGroups`、
`model.Token.SetAutoGroups/GetAutoGroups`、3 个 i18n key 都在），功能内聚，故一并接入：

- `tokenAutoGroupsInput`（三态：没带字段 = 保持原值 / `null` / 空数组 = 清空继承）
- `tokenRequest` / `tokenResponse` 包装类型，`buildMaskedTokenResponse` 返回类型改为 `*tokenResponse`
- `setTokenAutoGroups`：数量上限 + 去重 + 用户可选分组校验
- `getTokenRequestUserGroup` / `GetTokenAutoGroups`（`GET /api/token/auto-groups`）
- `AddToken` 改绑 `tokenRequest`，`UpdateToken` 加三态分支

**main-alpha 的企业主账号 apikey 限制 2 处 + `Int64Flexible` + `capTokenLimitsForEnterpriseSubAccount` 全部原样保留。**

### 11.5 relaykit golden 快照重新生成

`TestGolden{Response,Stream}ConversionMatrix` 失败，原因是 main-alpha 的 dto 与官方有**两处有意差异**：

| 差异 | main-alpha | 官方 |
|---|---|---|
| `claude_cache_creation_{5_m,1_h}_tokens` | 带 `,omitempty`（零值不输出给客户端） | 无 omitempty |
| `accepted_prediction_tokens` / `rejected_prediction_tokens` | **有**（Predicted Outputs 支持） | 无 |

golden 是官方快照，`go test ./relayconvert/ -run TestGolden -update` 重新生成。
重生成后 diff 只含上述两项（非零的 `claude_cache_creation_5_m_tokens: 2` 仍正常输出，确认 omitempty 只吃零值）。

⚠️ **以后每次合官方，只要动了 relaykit/dto 的字段就要重跑 `-update`，并逐行看 diff 是不是只有 main-alpha 的有意差异。**

## 十二、最终验证结果

| 项 | 结果 |
|---|---|
| `go build ./...`（主 module） | ✅ |
| `go vet ./...`（主 module） | ✅ |
| `relaykit` build + vet + test | ✅ **0 失败** |
| `go mod tidy` ×2 | ✅ 无改动（go.mod 本就正确） |
| `go test ./...`（主 module） | **19 失败**，见下 |
| 计费符号机械核对 17 项 | ✅ 全在；无 `CachedCreationTokens + CacheWriteTokens` 相加写法 |
| 防丢清单第九节 | ✅ 全过（含 RuoYi JWT 5 项、双前端 6 项、OperLog 4 项、FIELD_LABELS 23 字段） |
| 关键回归测试 | ✅ `TestSumUsedQuotaDoesNotResetQuotaByRpmTpmScan` / `TestLogMarshalJSONIDsAreString` / `TestFormatClaudeResponseInfo_*` ×5 / `TestHandleStreamResponseData_SetsClaudeWebSearchRequestsCtxKey` 全 PASS |

### 12.1 19 个失败 = 阶段3 基线 17 + 新增 2

| 包 | 数量 | 状态 |
|---|---|---|
| `model` | 10 | 基线（SQLite `AutoMigrate(&Channel{})` DDL 问题，生产用 MySQL 不受影响） |
| `relay/channel/gemini` | 4 | 基线（官方 BillingUsage 辅助链未吸收） |
| `relay/channel/claude` | 2 | 基线（阶段0 遗留） |
| `service` | 1 | 基线（`TestTaskBillingOtherIncludesVideoBillingFields`） |
| **`relay/channel/aws`** | **2** | **新增** |

**新增的 aws 2 个**（`TestAwsStreamHandlerUsesFinalUpstreamUsage`、
`TestAwsStreamHandlerStopsAtClientCancellationAndKeepsPartialBillingUsage`）：
断言 `usage.BillingUsage != nil`，与 gemini 那 4 个**同源**——官方的 BillingUsage
挂载链在我们取 ours 的 claude/gemini 实现里没有。

**不会算错钱**：`service/billing_usage.go` 的 `effectiveBillingUsage()` 在
`BillingUsage == nil` 时回落到旧 usage 字段，正是 main-alpha 一直走的计费路径。
这 6 个测试会随「claude/gemini 定制迁进 relaykit」一并解决。

### 12.2 顺带核实：`AllowNegativeBalance` 从 6 处变 4 处不是丢失

防丢清单写「6 处扣费预检查放行判断」，现在只有 4 处。核实结论：
`service/pre_consume_quota.go`（含其中 2 处）整个文件在**阶段2** 被官方的
`refactor: 抽象统一计费会话 BillingSession`（`116004fd4`）取代，
`PreConsumeQuota` 函数定义和调用**全部消失**，其职责由 `service/billing_session.go`
承接——而那 2 处检查在 `billing_session.go:370/376` 都在。

**当前 4 个额度检查点全部带欠费放行，无遗漏**。清单第九节该项数字已订正为 4。

## 十三、遗留（转长期待办）

1. **claude/gemini 的 2117 行定制仍未迁进 relaykit** —— 阶段2/3/4 都是取 ours 绕过。
   这是 relaykit 架构对齐的最后一块债。做完能一次性解决 gemini 4 + aws 2 共 6 个失败测试。
   需按 `convmeta.Meta` / `convmeta.Options` / `kitutil` 重写，且**必须真机验证计费**。
2. **`web/src/` 是死代码** —— 官方断崖② `31d70fca3` 摊平前端时带进来的 53 个文件
   （连同 `web/index.html`、`web/rsbuild.config.ts`、`web/tsconfig*.json`）。
   我们实际用的是 `web/default/` + `web/classic/`，`main.go` 的 4 个 `//go:embed`
   也只指向这两个的 `dist`，所以**不参与构建、不影响运行**。
   阶段4 的 merge 又更新了这 53 个文件（纯噪音）。
   要么将来做前端摊平迁移时以它为基础，要么直接删掉——**待主人决定**。
3. SQLite `AutoMigrate(&Channel{})` DDL 根因未定位（生产 MySQL 不受影响）。
4. `TestTaskBillingOtherIncludesVideoBillingFields` 可能是真实的计费展示 bug。
5. Claude `ContentTypeFile` → text/document 转换（阶段0 遗留 2 个测试）。
6. auth 无状态 token + 会话管理迁移（阶段3 已拒，需连带保住 RuoYi JWT）。

## 十四、🔴 上线前必须真机验证（第七节清单，一项不能少）

阶段4 动的是**协议转换 + 计费核心**，改错不会 build fail，会**静默算错钱**。
第七节那 9 项真机验证**尚未执行**，是上线前的硬门槛。
