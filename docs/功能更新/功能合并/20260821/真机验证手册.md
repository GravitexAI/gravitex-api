# 20260821 合并真机验证手册

本批合官方 47 commit（`2d61328c1`），动了**额度预留 / 并发扣费 / MJ 计费 / 鉴权限流**等计费与鉴权核心。离线已验证：`go build ./...` ✅、`relaykit GOWORK=off go build` ✅、`go vet ./...` ✅、model 测试 FAIL 集与合并前 tip 完全一致（均为环境依赖既有失败，**零新增**）✅、防丢清单第九节 ~45 项全通过 ✅。

下列需**真机**（差分/单测测不到"真实并发 + Redis + 批量刷新"时序）：

| # | 验证项 | 步骤 | 通过标准 | 结果 |
|---|---|---|---|---|
| 1 | 🔴 批量模式下额度预留不超扣 | `BATCH_UPDATE_ENABLED=true` + Redis 开启。同一用户高并发发请求，使缓存扣减快于 DB 批量刷新 | 余额扣到 0 后再无请求被放行；DB 最终余额不为负。**不能**因降级读未刷新的旧 DB 余额而超扣 | ☐ |
| 2 | 🔴 user-quota reserve 缓存路径生效 | 冷缓存下首次扣费（cache miss → 同步水合 → 重试） | 日志无持续 "user quota cache reserve unavailable, falling back to database"；`GetUserCache` 返回的 Quota 随扣费递减 | ☐ |
| 3 | 🔴 MJ 任务计费不双扣 | 提交 Midjourney/SwapFace 任务并完成 | 账单只扣一次（无 fork defer + 官方 Settle 双扣） | ☐ |
| 4 | access-token 生成限流 | default 前端「个人设置」反复点生成 access token | 触发 `UserCriticalRateLimit` 限流提示，不被缓存 | ☐ |
| 5 | 渠道测试（worker 池 + 自动禁用/启用） | 后台批量测试渠道，含 1 个坏渠道 + 1 个恢复的渠道 | 坏渠道自动禁用、恢复渠道自动启用；测试日志带 requestId | ☐ |
| 6 | Claude/Gemini 原生格式渠道测试 | 测 Claude(`/v1/messages`)、Gemini(`:generateContent`) 渠道 | 用原生请求格式测试通过 | ☐ |
| 7 | 🔴 RuoYi SSO 不受影响 | 从 Java 管理端跳转进 Go 界面 | 免登录成功 | ☐ |
| 8 | 🔴 A2 default 前端登录 | default 前端账号密码登录 | 不再无限刷新，`/api/data/self`、`/api/user/models` 正常 | ☐ |
| 9 | 用户资料更新不覆盖并发变更 | 编辑用户资料的同时发生扣费/token 轮换 | 额度扣减、aff、access_token 不被资料更新的旧快照覆盖 | ☐ |
| 10 | classic 渠道启停/编辑 | classic 前端启用/禁用/编辑渠道 | 正常（POST /:id/status） | ☐ |

## 快速自查（部署前）
```bash
# 编译三关
go build ./... && (cd relaykit && GOWORK=off go build ./...) && go vet ./...
# 防丢关键校验
grep -rn "AuthVersion\|IncrementUserAuthVersion" --include="*.go" model/   # 应空
grep -rn "CompactModelSuffix\|WithCompactModelSuffix" --include="*.go" .    # 应空
grep -c tryRuoYiJWTAuth middleware/auth.go                                  # 应 2
grep -c 'BillingUsage \*BillingUsage `json:"-"`' relaykit/dto/*.go          # 应 3
```

## 结果汇总
| 项 | 结论 | 验证人/日期 |
|---|---|---|
| 1-10 | ☐ 待真机 | |
